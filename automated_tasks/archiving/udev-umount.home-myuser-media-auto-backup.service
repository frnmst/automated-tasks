[Unit]
Description=Umount home-myuser-media-auto-backup after archive-media-files.mypurpose.service finishes

[Service]
Type=oneshot
ExecStart=/usr/bin/systemd-inhibit /usr/bin/bash -c 'while [ $$(systemctl is-active --quiet archive-media-files.mypurpose.service; echo $${?}) -eq 0 ]; do sleep 1; done; sync && systemctl stop home-myuser-media-auto-backup.mount'
User=root
Group=root
